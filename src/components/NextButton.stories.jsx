import NextButton from "./NextButton";

export default {
    title: 'components/NextButton',
    component: NextButton,
} 

const Template = () => <NextButton/>;

export const Default = Template.bind({});